# 今晚吃什麼？晚餐拉霸

一個不需要資料庫、可直接離線開啟的隨機晚餐選擇網頁。

## 使用方式

直接用瀏覽器開啟 `index.html`，按下「開抽」即可同時抽出料理類型、餐點與吃法。

- 可分別挑選料理、餐點與吃法的候選項目
- 可新增或刪除自訂選項，也能只重新抽餐點
- 所有選項只儲存在瀏覽器的 `localStorage`
- 純 HTML、CSS、原生 JavaScript，沒有外部套件或後端
